/**
 * 配置编译环境和线上环境之间的切换
 *
 * configBaseUrl: 域名地址
 * routerMode: 路由模式
 *
 */

let configBaseUrl = '';//测试环境接口域名
let configHTMLURL = '';//测试环境前端页面域名
let appId='';//测试的appid

export {
  configBaseUrl,
  configHTMLURL,
  appId
}
